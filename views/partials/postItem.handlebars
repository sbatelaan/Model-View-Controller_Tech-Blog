
<div class="card">
  <div class="card-header  flex-row justify-space-between">
    <h2>
      <a href="/post/{{id}}">
        <div class="card--post--link">{{postTitle}}</div>
      </a>
    </h2>
    <div class="card--post--title">posted by {{user.username}} on {{format_date dateCreated}}
    </div>
  </div>  
  <p class="card-body">
   {{postContent}}
  </p>
</div>